{% extends 'layout.html.twig' %}

 {% block content %}
     <div class="form-group row">
         <div class="col-md-8 offset-md-2 formMaterial" style="margin-top: 150px">
             <div class="form-material form-material-info floating">
                 {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                     {{ form_start(form) }}
                     <button type="submit" class="col-lg-4 offset-lg-4 btn btn-alt-primary submit">Ajouter</button>
                     {{ form_end(form) }}
                 {% endif %}
             </div>
         </div>
     </div>

    <script src="{{ asset('js/AddTricks.js') }}"></script>
     <script src="{{ asset('js/AddMovies.js') }}"></script>

 {% endblock %}
