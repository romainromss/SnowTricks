doctrine:
    dbal:
        driver: 'pdo_sqlite'
        server_version: '%env(DB_VERSION_TEST)%'
        charset: '%env(DB_CHARSET_TEST)%'
        memory: true
        url: '%env(resolve:DATABASE_URL_TEST)%'
